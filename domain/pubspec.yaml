name: domain
description: A sample command-line application.
version: 0.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  build_runner: ^2.3.0
  lints: ^3.0.0
  test: ^1.16.0
dependencies: { equatable: ^2.0.5}
